# marketing-assets

SNS 마케팅 에셋(카드뉴스·커버·썸네일) 제작 리포지토리.
현재 기준 캠페인: **도쿄 2박3일 2026 여름 가이드** (`sns/images/`) — 모든 신규 에셋은 이 캠페인의 품질·스타일을 기준선으로 삼는다.

## 가장 중요한 규칙 (모델 성능과 무관하게 품질을 보장하는 핵심)

1. **한글/텍스트가 들어가는 이미지는 절대 이미지 생성 모델로 직접 만들지 않는다.**
   텍스트·레이아웃은 HTML/CSS로 조판하고 Playwright(Chromium)로 스크린샷을 떠서 PNG를 만든다.
   이미지 생성 모델은 **배경 사진 소재**를 만들 때만 사용한다.
2. 카드뉴스·커버·썸네일 작업을 시작하기 전에 반드시 스킬을 먼저 읽는다:
   `.claude/skills/sns-card-news/SKILL.md` — 디자인 토큰, 레이아웃 스펙, 카피 공식, 렌더링 파이프라인, QA 체크리스트가 전부 정의되어 있다.
3. 스킬에 정의된 디자인 토큰(색·폰트·간격)에서 임의로 벗어나지 않는다. 새 캠페인이라도 토큰을 교체하는 것이지, 구조를 무시하는 것이 아니다.
4. 결과물을 커밋하기 전에 렌더된 PNG를 **Read 툴로 직접 열어 눈으로 검수**한다(텍스트 잘림, 오버플로, 대비 부족 확인). 검수 없이 커밋 금지.

## 디렉토리 구조와 네이밍

```
sns/
  images/              # 최종 산출물 PNG (커밋 대상)
    thumbnail.png      # 블로그/링크 썸네일 1200×630
    sns_cover.png      # SNS 커버 1200×630
    card_news_1.png    # 카드뉴스 1080×1080, 1번은 훅(질문) 카드
    card_news_2~4.png  # Day별 상세 카드
    card_news_5.png    # 요약 + CTA 카드
```

- 새 캠페인은 `sns/<campaign-slug>/images/` 아래에 같은 네이밍으로 생성한다 (예: `sns/osaka-3days/images/`).
- 중간 산출물(HTML 템플릿, 배경 소재, 스크립트)은 스크래치패드에서 작업하고, 최종 PNG만 리포지토리에 넣는다. 단, 재사용 가치가 있는 템플릿 수정은 `.claude/skills/sns-card-news/templates/`에 반영한다.

## 산출물 규격 (고정)

| 파일 | 크기 | 용도 |
|---|---|---|
| `thumbnail.png` | 1200×630 | 블로그/오픈그래프 썸네일 |
| `sns_cover.png` | 1200×630 | SNS 피드 커버 |
| `card_news_N.png` | 1080×1080 | 인스타그램 카드뉴스 (5장 1세트) |

렌더는 항상 `deviceScaleFactor: 2`로 찍은 뒤 목표 크기로 리사이즈하거나, viewport를 목표 크기로 두고 2배 스케일로 찍어 선명도를 확보한다(스킬의 렌더 스크립트 참조).

## 작업 순서 (요약 — 상세는 스킬 참조)

1. 스킬 읽기 → 캠페인 정보(목적지, 일정, 맛집, 가격, 시즌 팁)를 **구체적 숫자와 함께** 정리
2. 카피 작성 (스킬의 카피 공식 사용 — 숫자·검증 포인트 필수)
3. 배경 사진 소재 확보 (이미지 생성 MCP 또는 기존 소재)
4. HTML 템플릿 채우기 → Playwright 렌더
5. PNG를 Read로 열어 QA 체크리스트 통과 확인 → 수정 반복
6. 최종본만 `sns/.../images/`에 저장 후 커밋

## Git 규칙

- 커밋 메시지: `feat: add SNS card images for <캠페인명> campaign` 형식 (기존 히스토리와 일관성 유지)
- PNG는 최종 검수 통과본만 커밋. 중간 버전을 커밋했다 덮어쓰지 않는다 (바이너리 히스토리 비대화 방지).
- 지정된 작업 브랜치에서만 작업하고 `git push -u origin <branch>`로 푸시한다.
